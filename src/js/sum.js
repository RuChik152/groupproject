function sums()
{
    sum = 0;
    for (i=1; i<2; i++) {
        var elem = document.getElementById("r" + i);
        if(elem)sum = sum + parseInt(elem.value);
    }
    dicument..content_basket_panel_item_panel.sumOut.value=sum
}